.catalog:
  name: http-test
  version: 0
  description: |
    DEMO to test adding catalog item
  uuid: httpd-test-1
  questions:
    - variable: "VERSION"
      label: "image version"
      description: "Specify a version of the container to pull (default will pull latest stable version)."
      type: "string"
      default: "napster-http-test-v1" 
      required: true
